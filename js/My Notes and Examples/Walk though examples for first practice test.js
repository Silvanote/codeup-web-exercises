function isFalse(example){
    return example === false;

}

function isTrue(trueexample){
    return trueexample === true;

}

function not(not){
    return !not;
}

function addOne(extraone){
    return parseFloat(extraone) + 1
}

function isEven(num) {
    if (isNaN(parseFloat(num))) {
        return false;
    } else {
        return parseFloat(num) % 2 === 0;
    }

    function isIdentical(y1, y2) {
        return y1 === y2;
    }

    function isEqual(x1, x2) {
        return x1 == x2;
    }

    function or(x, y) {
        return x || y;
    }
}
    function and(x, y) {
        return x && y;
    }
}
    function concat(x, y){
        return "" + con + cat
    }
}
    // another example concat
    // function concat (con, cat){
//  return con.tostring() + cat.tostring();

// another example
// function concat(con,cat){
// return "" + con + cat;
//}

